{
  "name": "site-hazard-spotter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:server": "npm install --prefix server",
    "dev": "npm run install:server && npm run dev --prefix server",
    "start": "npm run start --prefix server"
  }
}
